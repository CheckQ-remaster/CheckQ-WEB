import React from "react";
import * as H from "./Header.style";

const Header = () => {
  return (
    <H.Container>
      <nav>
        Header
      </nav>
    </H.Container>
  )
};

export default Header;
